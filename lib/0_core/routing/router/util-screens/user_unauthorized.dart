import 'package:flutter/material.dart';
class UserUnauthorized extends StatelessWidget {
  const UserUnauthorized({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
